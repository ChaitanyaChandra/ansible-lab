    location / {
        proxy_pass http:{{SERVICE}}-{{ENV}}.{{DOMAIN}}:8080;
    }

    location /health {
      return 200 'OK';
    }

    location /nginx_status {
        stub_status on;
        access_log off;
    }
#