[Unit]
Description = {{COMPONENT}} Service
After=network.target

[Service]
User={{USER}}
ExecStart=/bin/node /home/{{USER}}/{{COMPONENT}}/index.js
SyslogIdentifier="{{COMPONENT}}"
StandardOutput=syslog
StandardError=syslog

[Install]
WantedBy=multi-user.target